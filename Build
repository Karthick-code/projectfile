
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Build Success'
            }
        }
        stage("upload") {
            steps{
                def inputFile = input message: 'Upload file', parameters: [file(name: 'newtext.txt')]
                echo "$inputfile"
            }
        }
    }
}
